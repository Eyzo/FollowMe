<nav class="navbar navbar-expand-lg navbar-light bg-style">
    <a class="navbar-brand" href="{{ path('main') }}">Follow Me</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

            <div class="search-container">
                <form class="form-inline my-2 my-lg-0">

                    <input class="search form-control mr-sm-2" id="search-user" type="search" placeholder="Recherche ..." aria-label="Search">

                    <button  class="btn btn-search" type="submit">
                        <span class="search-icon">
                            <i class="fas fa-search"></i>
                        </span>
                    </button>

                </form>

                <div class="result">

                </div>
            </div>

            <div class="links-group">
                {% if app.user %}
                    {% if app.user.profile %}
                        <li class="link nav-item active">
                            <a class="nav-link" href="{{ path('my.followers') }}">Mes Followers</a>
                        </li>
                        <li class="link nav-item active">
                            <a class="nav-link" href="{{ path('my.follows') }}">Mes Follows</a>
                        </li>
                    {% endif %}
                    <li class="link nav-item active">
                        <a class="nav-link" href="{{ path('my.profile') }}">Mon Profile</a>
                    </li>
                {% endif %}
            </div>
        </ul>
        <div>
            <ul class="navbar-nav">
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">se Déconnecter</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connectez vous</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>